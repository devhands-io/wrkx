title: Add progress bar to test run output
status: todo
depends: []

Steps:
- After the thread calibration output, print a pseudo-graphical progress bar
  that refreshes in-place every second
- The bar must show elapsed time, total duration, and completion percentage, e.g.:
    Progress: [=========>          ] 45% (9s / 20s)
- Use ANSI cursor-up / carriage-return to overwrite the same line each second
  so it does not flood the terminal
- The bar should disappear (or finalise) cleanly when the run completes and
  the results section is printed

Acceptance:
- Running wrkx with a duration shows a live-updating progress bar on one line
- The bar appears immediately after calibration output, not before it
- The results section prints normally after the bar clears
- `make test` passes, 0 failures
