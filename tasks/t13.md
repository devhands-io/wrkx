title: E2E error counting test
status: todo
depends: t6

Steps:
- Create tests/e2e/errors.sh:
    - Start mock_server.py on port 18083 in `flaky` mode (10% 503 responses)
    - Wait for server readiness
    - Run: wrkx -t1 -c10 -d5s -R50 http://localhost:18083/
    - Capture stdout; assert exit code 0
    - Assert "Non-2xx or 3xx responses:" appears in output
    - Extract the count from that line; assert count > 0
- Add to Makefile test-e2e: @bash tests/e2e/errors.sh

Acceptance:
- `make test-e2e` exits 0
- Error line appears and count > 0 (expected ~25 errors from ~250 requests at 10% flaky)
- Script fails if only 200s are returned
