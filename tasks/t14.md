title: Coverage via gcov
status: todo
depends: t3, t4, t5

Steps:
- Add COVFLAGS := --coverage -O0 -fno-inline to Makefile
- Thread $(COVFLAGS) into each TEST_*_BIN $(CC) recipe
- Add coverage Makefile target:
    - Re-runs all unit-test binaries to generate .gcda data
    - Calls: gcov -r src/stats.c src/units.c
    - Asserts >=80% line coverage for each (parse gcov output)
- Optionally add coverage-report target using gcovr --print-summary
- Add to .gitignore: *.gcda  *.gcno  *.gcov  obj/*.gcda  obj/*.gcno
- Add coverage and coverage-report to .PHONY

Acceptance:
- `make coverage` exits 0
- stats.c and units.c each show >=80% line coverage
- Running twice in a row succeeds (gcda files overwrite cleanly)
- gcov artifacts absent from `git status`
