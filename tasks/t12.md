title: E2E reconnect robustness test
status: todo
depends: t6

Steps:
- Create tests/e2e/reconnect.sh:
    - Start mock_server.py on port 18082 in `drop` mode (abrupt close after accept)
    - Wait for server readiness
    - Run wrkx in background:
        wrkx -t1 -c5 -d8s -R10 http://localhost:18082/ &
        WRK_PID=$!
    - After 4s, sample CPU%: ps -o %cpu= -p $WRK_PID
    - Assert CPU% < 90 (pre-patch bug pegged a core at ~100%)
    - If wrkx already exited, fail with a clear message (crash is not a pass)
    - wait $WRK_PID; assert exit code 0
    - Use trap EXIT to kill wrkx if still running
- Add to Makefile test-e2e: @bash tests/e2e/reconnect.sh

Acceptance:
- `make test-e2e` exits 0
- CPU sample < 90% with reconnect patch active
- Reverting the reconnect patch (wrk2/pull/33 in wrk.c) makes this test fail
- wrkx exits cleanly (code 0) after the run duration
